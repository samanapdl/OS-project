This zip folder contains all the required java activity and xml files to run the application.

It also includes manifest.xml which is equally critical as it holds the activity relationship.

Zip contains a drawable resource folder which contains all the images used in the app.

As the zipped size of the whole project was too large we only included the essential files used 
for the project.

